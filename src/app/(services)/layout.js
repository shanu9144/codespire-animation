/**
 * Services Pages Layout
 * Provides layout and SEO configuration for service pages
 * 
 * @module app/(services)/layout
 */

export default function ServicesLayout({ children }) {
  return <>{children}</>;
}

