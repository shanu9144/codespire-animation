/**
 * Marketing Pages Layout
 * Provides layout and SEO configuration for marketing pages
 * 
 * @module app/(marketing)/layout
 */

export default function MarketingLayout({ children }) {
  return <>{children}</>;
}

