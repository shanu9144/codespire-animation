/**
 * Products Pages Layout
 * Provides layout and SEO configuration for product pages
 * 
 * @module app/(products)/layout
 */

export default function ProductsLayout({ children }) {
  return <>{children}</>;
}

